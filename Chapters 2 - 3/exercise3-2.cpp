#include <iostream>
using namespace std;
#define endl "\n";

/*
*   Rewrite the code that finds the agent with the best monthly sales average,
*   so that it finds the highest median sales.
*   The median of a set of values is the one "in the middle", 
*   such that half of the other values are higher, and half of the other values are lower. 
*   If there is an even number if values, the median is the simple average of the two values in the middle.
*   In the set 10, 6, 2, 14, 7, 9 the values in the middle are 7 and 9, the average of 7 and 9 is 8, being the median.
*/

int main(void)
{


    
    return 0;
}